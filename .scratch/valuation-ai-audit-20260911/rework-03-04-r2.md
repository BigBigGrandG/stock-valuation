# Issues 03/04 — R2 acceptance rework (2026-09-12)

## Goal
Complete the original implementation-03-04.md acceptance contract without reopening 01/02 or implementing 05–08. R1 delivery lifecycle succeeded, but coordinator product acceptance is REJECTED pending the items below.

## Baseline / Evidence
- master / d0a7d62, origin/master same. Existing R1 tracked changes and untracked multiples.py/tests/reports are preserved; no commit/push/clean/reset.
- R1 task task_e657b5846821 / dispatch ctx_ef7ff6adc26d, completed and settled. Continue from current files, not from a clean tree.
- Coordinator independently ran `.\.venv\Scripts\python.exe -m pytest -q` in D:\workshop\stock-valuation: exit 0, 356 passed, 2 dependency deprecation warnings, 4.07s. This confirms regression status, not missing financial assertions.
- Local inspection: multiples.py COMPANY_HISTORY_MIN_COVERAGE_DAYS=365; history only checks price_as_of/enterprise_value_as_of, not forecast vintage; missing is_forward accepted. Coverage determined before outlier filtering; duplicate dates count as observations. Industry metric validation does not check denominator accounting/forecast basis. R1 report acknowledges all-firms observed EV aggregate and full browser suite 5 passed / 1 failed.

## Scope / Ownership
Same single-worker end-to-end ownership as original contract: relevant backend, frontend, tests and evidence. Include the relevant failing fullstack fixture/test repair; it is within verification scope, not a separate deferred task. Coordinator owns this contract and final acceptance. Worker may update issues 03/04 to in-progress/pending acceptance, never resolved. No nested worker.

## Constraints / Required corrections
1. **Industry basis safety**: observed all-firms EV/EBITDA must not automatically become a valid forward operating-EBITDA target. Require machine-checkable basis compatibility; incompatible/unknown candidates fall back independently with explicit reason. A separately named evidence-based configured industry assumption is allowed ONLY with documented defensible calibration and conservative downgrade; merely renaming an unchanged incompatible ratio is insufficient. If no defensible calibration exists, reject EV candidate and retain explicit system fallback; compatible industry PE can still provide meaningful differentiation. Preserve model applicability gates and FCFE-yield fallback warning requirement.
2. **History point-in-time safety**: historical prediction vintage must be proven available on/before observation date and target fiscal period must actually be forward at that date. Require explicit forward evidence, compatible currency/unit/per-share or EV/EBITDA accounting basis; missing evidence degrades, never current forecasts attached to old prices. HTTPS plus FY text alone is insufficient. Implement the original 3–5 YEAR coverage, not 3–5 samples over one year. Deduplicate observations; validate coverage/count/age and provenance on the retained post-filter set. Document and test sampling/outlier policy including zero-MAD, repeated dates, dense history and outlier endpoints.
3. **Industry mapping**: remove ambiguous substring-first matching. Unknown/broad labels must not select an arbitrary row. Validate aliases against documented upstream/source definitions, especially Internet Content & Information -> Software (Internet). This mapping's economic validity is NOT VERIFIED by coordinator; provide primary-source evidence or degrade rather than assume. Source classification must be auditable and not ticker-white-listed.
4. **Browser acceptance**: fix the reported incomplete FCFF fixture/stale expected value without weakening production 01/02 eligibility or deleting/skipping assertions. Prove parameter edit/override, reset, ticker switch and export with new selected source layers/fade. Re-run relevant fullstack suite and frontend typecheck/lint/build, record actual commands/cwd/exits.
5. **Evidence completion**: original requirement is four core tickers PLUS at least two different-industry edge cases, bounded live or explicitly labelled historical live payload replay. Preserve raw inputs and new responses, not only summaries; list per-metric layers/compatibility/degradation and DCF eligibility/trajectory. Store reproducible scripts and primary source evidence (table column definitions, dates, methodology and mapped classifications); do not substitute demo. Explain network limitations honestly. Record which exact acceptance items PASS/FAIL/NOT RUN.
6. Preserve R1 DCF fade work; coordinator local diff found no new confirmed DCF formula defect. Maintain first two explicit FCFF, g5 terminal equality, sensitivity rebuild and numerical tests. Avoid unnecessary rewrite.

## Acceptance / Stop
Red regression for identified safety holes, green targeted and full offline backend; frontend/browser checks above. Full source -> normalizer -> assumptions -> engine -> API/export proof, per original contract. Report all commands/cwd/exits and changed files in implementation-report-03-04-r2.md; artifacts under verification/03-04-r2/. Retain R1 evidence unchanged. Runtime: reuse Codex gpt-5.6-luna max per explicit user preference; current Antigravity quota not probed, resets_at unknown. No model fallback within this task assumed.

Ordinary implementation autonomous. If a major financial calibration cannot be supported, use conservative fallback as above or ask a concrete Orca question; do not fabricate data. Send exactly one standard worker_done for NEW task/dispatch with explicit outcome and three-sentence summary, then stop. Coordinator stops turn once only waiting remains.
