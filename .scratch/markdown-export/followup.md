Goal: Close specific Markdown export acceptance evidence gaps. Same frontend worker, original feature ownership/constraints. No backend changes or commits. Do not re-explore unrelated repo.
1. Report claims .scratch/markdown-export/evidence/sample_nvda_export.md exists; coordinator read failed (file not found). Verify every listed artifact link/path; create or correctly link actual sample .md. Record real frontend PID/start/build identity and source hashes, not 'PID: rebuilt'.
2. browser_test_export.py only has normal NVDA/TSM download, applied override/reset substring assertions and error state. Add executable proof for unsaved P/E edit (old applied result/assumptions stay accurate, unsaved value not falsely exported), export during loading/recalculation/cancellation and delayed ticker switch (no stale/other-company export), and post-switch correct result. Assert exact changed/reset exported numerical result AND effective parameters, not merely presence of 25.00x or 20.00x somewhere. Keep zero-extra-API-call checks.
3. Ensure complete-page coverage includes all scenario intermediate values for non-DCF models as shown on page, warnings even for unavailable models, assumption sources/applied status, and full visible source/provenance fields. Review your matrix against actual page sections. Report any omissions and fix them, no raw-JSON substitution.
4. Original task requires escaping backticks as well as pipes/newlines. Add meaningful special-character fixture proving readable Markdown (table and headings/prose) for backticks/pipes/newlines in company/source/notes; fix if needed. Preserve plain financial values and UTF-8 Chinese.
5. Run focused formatter/browser tests, and frontend typecheck/lint/build if implementation changes; update production3000 if changed. Update acceptance report with truthful assertions, counts/exit codes, sample and screenshot paths. Feature is not fully accepted until these are demonstrated. Return only outcome, changed files, core changes, verification evidence/diff stat/risks. Use fresh lifecycle only, never run-use/self-dispatch.
