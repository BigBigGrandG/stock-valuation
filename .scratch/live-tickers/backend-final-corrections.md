Goal: Fix unmet acceptance from backend-final-task.md; reuse existing worker and preserved changes. Same ownership/constraints. Coordinator has NOT accepted your final report.

Specific failed evidence and required corrections:
1. valuation_service.py lines 117-121 only checks USD, but report claims US exchange evidence. Currency is not listing geography. Carry actual provider exchange/market evidence through the validation path (compatible optional fields if necessary), distinguish non-US USD listings, unknown listing evidence, and foreign-headquartered US ADRs. Test real provider -> normalizer path for these cases. Do not call USD alone proof of a US listing.
2. test_ticker_bundle_lock_deduplication manually acquires bundle._lock and writes _info, bypassing get_info entirely. Replace with concurrent calls to production get_info/provider methods and assert one actual mocked upstream fetch, including error/retry and bundle/category TTL interactions.
3. test_upstream_total_timeout_enforcement only backdates fetched_at and invokes _check_deadline. Actual get_info calls self.ticker.info synchronously without a deadline and blocking lock is unbounded. Enforce elapsed bound on real public provider/request path even while upstream is blocked, including queue/lock/semaphore time. Use controlled blocking fake upstream and a short injectable timeout; assert elapsed limit, typed failure, bounded resource usage and subsequent recovery. Cache age must NOT be the request clock: a partly cached valid bundle older than 25s but within TTL should still fetch missing categories with a fresh request budget. Test this explicitly. Preserve 429/503 classification.
4. Forecast date tests claim non-calendar coverage yet only use December 31. Code only compares years and assumes lastFiscalYearEnd+1 = 0y; no proof that estimates target that date. Verify exact fiscal end/estimate horizon from available upstream period metadata (read current primary docs/source as needed); never infer adjacency solely from year. Cover June/January fiscal endings, same-year date mismatch, stale/missing fiscal metadata, and a forecast 0y whose horizon differs from assumed next period. Missing/unproven alignment disables only affected derived forecasts with an accurate explanation. Cash-flow and EBITDA follow same rules. Summary forwardEps may keep honest provider-unspecified horizon, but do not silently propagate its label to independent revenue-derived forecasts.
5. TSM per-ADS EPS/share currency evidence must be grounded in actual raw provider data or a primary source, not comments or synthetic fixtures. Preserve exact raw fields and mapping and cite source. If basis cannot be proven, leave only affected model unavailable with honest reason. No invented FX or ADR ratio.
6. Re-run meaningful regression/full pytest, live oracle, strict special tickers, reconciliation. Correct backend-final-acceptance.md to match evidence; include precise final service identity/restart time and source hashes, commands/exit codes, changed files and available diff stat. Existing no-commit baseline limitation must be explicit. Do not resolve issue.

Return only updated outcome/changes/verification/diff stat/risks. Send heartbeat on this fresh dispatch; never run-use or self-dispatch. No frontend edits. Fix end-to-end; do not merely rewrite tests to match current behavior.
