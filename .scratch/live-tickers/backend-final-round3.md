Goal: Resolve remaining concrete acceptance failures; same backend worker, scope and constraints as backend-final-corrections.md. No frontend edits, no commits/push, no run-use/self-dispatch. Complete implementation and regression end-to-end.

1. Real bounded upstream resources: _fetch_property currently submits to ThreadPoolExecutor and releases _SEMAPHORE in finally even when future.result times out and the upstream function continues running. A repeated timeout storm can enqueue unlimited work; same bundle can launch duplicate still-running property fetches. Keep admission accounting attached to actual future completion, bound submitted+running work, and reuse in-flight fetch per property as appropriate. Timed-out callers must return within deadline; do not pretend Python future timeout cancels running IO. Test with event-blocked upstream, requests exceeding executor/admission capacity, same ticker retries, no unbounded queued/running growth, and recovery AFTER upstream releases. Avoid tests that sleep forever or leave non-daemon executor jobs pending. Report explicit limits and tests.
2. Total incoming valuation budget: public provider methods each create fresh _RequestBudget, so seven serial category calls can each consume a new 25s. Verify the actual API/service aggregation path has ONE incoming valuation deadline including category cache, bundle locks, admission and nested fetches; implement if absent. Test cumulative short upstream delays across several categories through real service/API path exceeding one small injected total budget, not just get_quote alone. Ensure healthy cached and partial-cache requests work.
3. _verify_forecast_alignment lines 197-203 still invents next year if nextFiscalYearEnd absent, contrary to correction requirement. Remove unproven forecast horizon inference; use actual estimate horizon metadata or disable only affected derived forward values with clear reason. Also prove how the 0y estimate horizon maps to nextFiscalYearEnd; merely renaming nextFiscalYearEnd as 0y is not independent verification. Cover missing-next (last present), stale dates, non-calendar periods and date mismatch. Preserve supported core live models via genuine data, not fallback fabrication.
4. ADR currency evidence enforcement: report says raw earnings_estimate has currency USD, but production must actually validate per-row estimate currency and share basis instead of assigning quote currency to every EPS. Verify and add negative regression for non-USD or unknown currency ADR EPS, preserving valid USD/ADS TSM PE when grounded. Preserve raw source capture proving TSM EPS/price/ADS basis and mapping. Do not use hardcoded fixture comments as source proof.
5. Re-run root full pytest with DATA_PROVIDER=demo isolated; strict live core/special/reconciliation; update backend-final-acceptance.md with exact executable evidence, truthful limitations, hashes and service PID/start time. Frontend final suite will be repeated only after this backend version is accepted; do not declare global acceptance or resolve issue.

Return outcome, changed files, core changes, commands/exit codes/counts, diff stat or no-baseline limitation, risks/blockers only. Confirm heartbeat on fresh dispatch.
