# Backend acceptance revisions (coordinator)

Read spec.md and finish the backend before claiming completion. Current 47 tests are a starting point, not full acceptance.

1. POST contract MUST accept nested JSON exactly as requested: {"forward_pe":{"base":18},"fcf_yield":{"base":0.05},"dcf":{"wacc":0.095,"terminal_growth":0.035}}. Pydantic Decimal nested models extra=forbid, reject wrong types/nonfinite/unknown fields. Current flat fields silently ignore requested nested payload. Sparse base-only changes should recompute default relative low/high bounds when not explicitly provided so low<=base<=high; yield order inverse. User override MUST beat historical multiples. Validate every effective WACC>g; invalid override return422, not silent missing base DCF. Add integration tests.
2. DCF: use actual forward FCFF1 and FCFF2 for first years when available, then capped deterministic growth. For historical TTM fallback derive next year (do not relabel TTM as forecast). Annual projections must have actual FY labels and provenance. Bear/base/bull must stay ordered even with declining growth. Historical FCFE growth is not an appropriate unlabeled FCFF proxy. Explicit assumptions, caps, data sources/as_of/is_estimated for each. Terminal growth <=.05 configurable. Do not label fixture-derived growth analyst consensus.
3. Model input metadata: use nested FinancialMetric dictionaries for EVERY input including quote/shares/cash/debt/net_debt and each model multiple, yield, growth, WACC, terminal growth (source,source_type,period,as_of,is_estimated,unit,value). Flat values with only forward EPS provenance are insufficient. Keep aliases if necessary for old tests but publish input_metrics and assumption_metrics maps as a stable UI contract. Each DCF scenario include projection_metrics, growth_rate, PV years and complete formulas/steps. Add multiple_source=historical/fallback/user_override/peer; wacc_source=fallback/user_override/calculated. Snapshot must serialize net_debt key, not only net_debt_metric. DCF cannot substitute FCFE for FCFF.
4. Composite: use only complete three-scenario positive available models for a consistent renormalization strategy; exclude partial DCF entirely or explicitly mark DCF unavailable. Never count missing scenario as zero with full denominator. Expose current_price, fair_value_low/base/high,MOS/upside, normalized weights/formula/calculation steps. Weights and classification constants must have ONE operative source in config. Currently config constants are duplicated and classify hardcodes thresholds.
5. Validate quote>0,shares>0,cash>=0,debt>=0, no finite-invalid metrics. Distressed nonpositive equity must not divide by0 or produce nonsensical MOS; mark affected model unavailable with reason. Catch model-specific failures independently in ValuationService, preserve other results. Financial data validation failures distinct from provider network errors. Quality considers estimated/missing/provenance for non-demo; demo ALWAYS LOW.
6. Data architecture: actual seven provider methods used by FinancialDataService, separate Normalizer, separate ValuationService, cache interface (Protocol/ABC) injected with category TTL quote120s/statements86400/estimates43200/multiples86400 and monotonic clock injectable in tests. Balance/profile sections typed or checked. Support guard in normalizer/service based on company profile (country/security_type/sector/profitability), not ticker denylist only. Reusable tests for bank/insurance/REIT/SPAC/ETF/loss/non-US. No arbitrary AVGO data for other tickers. ProviderRateLimitError->429,Unavailable->503,notfound->404, invalid ticker->422. Stale all financial inputs warning, not quote alone.
7. Demo all metric source_type FIXTURE/is_demo and fixed dates; no fictional analyst consensus presented as actual source. Clearly separate mock analyst estimate concept in notes only. Include configurable fallback WACC fully labeled. No API keys needed. Expose health route and typed response in OpenAPI if practical.
8. Project reproducibility: pyproject.toml requires-python>=3.12, pinned/locked requirements or uv.lock, .gitignore root (.venv,node_modules,__pycache__,.next,env excluding examples), .env.example backend config. Write README backend sections with setup/start/tests/API nested examples/math/FCFF-v-FCFE/demo limitations. Use root .venv Python3.12, currently installed. Do NOT write frontend; another Antigravity owns it.

Run existing tests plus regression tests, start uvicorn in background with Start-Process -WindowStyle Hidden (avoid Start-Job lifetime issues), actual HTTP GET/POST/snapshot/invalid checks. Save latest AVGO JSON .scratch/valuation-mvp/avgo-response.json and snapshot.json and send status once schema ready for frontend. Final worker_done only when backend satisfies these points, with test counts/commands. Poll Orca mailbox each phase.
